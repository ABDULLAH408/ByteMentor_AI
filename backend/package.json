{
  "name": "bytementor-backend",
  "version": "1.0.0",
  "description": "Lambda backend for ByteMentor AI autonomous coach",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --minify --platform=node --target=node18 --outfile=dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-ses": "^3.500.0",
    "@aws-sdk/util-dynamodb": "^3.500.0",
    "@google/genai": "^2.12.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^18.19.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
